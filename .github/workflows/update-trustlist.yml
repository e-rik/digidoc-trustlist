name: Update trustlist

on:
  schedule:
    - cron: "0 2 * * *"
      timezone: Europe/Tallinn
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-trustlist:
    name: Update trustlist
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install xmllint
        run: |
          sudo apt update
          sudo apt install -y libxml2-utils

      - name: Download files
        env:
          TRUSTLIST_DOWNLOAD_DIR: ${{ runner.temp }}/files
          TRUSTLIST_PACKAGE: ${{ runner.temp }}/digidoc-trustlist.tar.gz
        run: |
          mkdir $TRUSTLIST_DOWNLOAD_DIR
          bash ./download-trustlist.sh
          tar -czf $TRUSTLIST_PACKAGE -C $TRUSTLIST_DOWNLOAD_DIR

      - name: Create or update rolling release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: latest
          TRUSTLIST_PACKAGE: ${{ runner.temp }}/digidoc-trustlist.tar.gz
        shell: bash
        run: |
          set -euo pipefail

          release_date="$(date -u +'%Y-%m-%d %H:%M UTC')"

          if gh release view "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then

            gh release edit "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --title "Trustlist update - $release_date" \
              --notes "Latest build from commit $GITHUB_SHA." \
              --prerelease

            gh release upload "$RELEASE_TAG" \
              $TRUSTLIST_PACKAGE \
              --repo "$GITHUB_REPOSITORY" \
              --clobber
          else
            gh release create "$RELEASE_TAG" \
              $TRUSTLIST_PACKAGE \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --title "Trustlist update — $release_date" \
              --notes "Latest build from commit $GITHUB_SHA." \
              --prerelease \
              --latest=false
          fi
